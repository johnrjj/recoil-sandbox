const palette = {
  green: '#43D787',
  blue: '#00AEEF',
  gray: '#A9AAAE',
  lightGray: '#F7F8FC',
  black: '#292B35',
  yellow: '#FFBB00',
  white: '#ffffff',
}

const sizing = {
  maxWidthPageWrapper: 1400,
}

const colors = {
  bgColor: palette.lightGray,
  headerBgColor: palette.white,
  tableCellColor: palette.white,
  primaryActionColor: palette.blue,
  primaryTextColor: palette.black,
  secondaryTextColor: palette.gray,
}

const theme = {
  palette,
  sizing,
  colors,
}

export { theme }
